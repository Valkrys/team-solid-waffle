import { BandsFilterPipe } from './bands-filter.pipe';

describe('BandsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BandsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
